import { MainPage } from './pages';

// todo do some router stuffs here.
// todo could wrapper <MainContainer /> and <Header /> inside MainPage to this app if u want.
const App = () => <MainPage />;

export default App;
